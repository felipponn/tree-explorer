#ifndef TREE_INFOS_H
#define TREE_INFOS_H
# include <iostream>

int height(struct Node*);

int size(struct Node*);

bool full(struct Node*);

bool perfect(struct Node*);

bool complete(struct Node*);

#endif